# silver-enigma

A lightweight project management tracker for:

- requirements
- design work
- development tasks
- QA rounds

## Usage

Open `index.html` in a browser from the project root to add, update, complete, and remove work items. Data is stored locally in the browser with `localStorage`.
